# AGENTS.md

## 项目定位与文档入口

本项目是医疗保障业务数据处理平台的管理端前端，项目目录为 `tools-react-yf`，主要承载系统用户管理、业务配置、数据筛选与匹配、统计汇总，以及 CSV 导入导出和异步任务结果查看。

进入项目后先读本文件，再按任务读取 `docs/share/` 和对应功能目录。任何描述都必须以当前代码为事实来源，不能把帮助文档、历史资料或设想写成已经实现的功能。

### 必读文档

- `docs/README.md`：文档总入口。
- `docs/share/项目概览.md`：项目范围、架构和业务地图。
- `docs/share/开发规范.md`：页面、服务、权限、CSV 和验证规则。
- `docs/share/接口规范.md`：当前接口响应、认证、分页和任务兼容规则。
- `docs/share/文档规范.md`：功能文档结构和维护要求。
- `docs/系统用户管理/`：后台登录账号管理。
- `docs/未救助台账/`：未救助明细、应补应退、下放通知和重大疾病编码。
- `docs/参保台账/`：年度配置、附件导入、台账计算、镇街复核和导出。

## 中文协作要求

- 默认使用简体中文沟通和编写文档、说明及业务注释。
- 代码标识符、接口字段、命令和框架专有名词可保留英文。
- 医保业务词必须沿用现有页面、接口和数据库中的名称，避免自行创造同义词。

## 当前技术栈

- React 18、TypeScript、Umi Max 4。
- Ant Design 5、Ant Design Pro Components。
- Umi `request` 负责大多数业务请求；登录和初始化流程存在原生 `fetch`。
- Day.js 处理日期；XLSX/FileSaver 用于部分前端表格导出。
- 当前台账导入只接受 CSV；大文件导入、筛查、导出由后端异步任务处理。

## 核心目录

- `.umirc.ts`：路由、代理、构建和布局配置。
- `src/pages/`：页面级业务模块。
- `src/services/`：业务 API 调用。
- `src/access.ts`：菜单和按钮权限映射。
- `src/app.ts`：初始化用户、统一请求、登录失效和全局布局。
- `src/components/TaskCenter/`、`src/components/TaskFloat/`：异步任务入口。
- `src/config/`：运行环境与 API 地址配置。
- `public/assets/templates/`：可下载 CSV 模板。

## 当前功能事实

- 系统用户管理入口是 `/user-management/accounts`，管理后台登录账号，不是参保人或救助对象。
- 未救助台账包含未救助明细、应补应退明细、下放通知、重大疾病编码。
- 参保台账包含参保台账明细、参保配置和参保导入记录。
- 镇街账号的数据范围由登录用户的镇街绑定和后端接口共同限制；前端隐藏并不替代后端鉴权。
- 当前接口成功码有历史差异：多数业务接口为 `0`，任务接口为 `200`。必须按 `docs/share/接口规范.md` 兼容，不能擅自改成其他项目的规范。

## 页面开发规则

1. 先读对应功能文档，再看相邻页面和服务文件。
2. 页面负责表单、表格、弹窗、权限可见性和调用编排；业务计算结果以后端为准。
3. 新业务请求优先放在 `src/services/`，不要继续扩大页面内直接请求的范围。
4. 路由需同步配置 `.umirc.ts`，按钮权限需同步 `src/access.ts` 和后端权限名。
5. 列表必须支持明确的加载态、空态、分页和错误提示；大字段使用省略与气泡，不撑开表格。
6. 身份证号、银行卡号、联系电话等敏感字段不得新增日志输出、本地持久化或无权限展示。

## CSV 与异步任务规则

- 台账导入只允许 `.csv`；模板表头、年份/月度或清算期必须在提交前明确。
- 不在前端复制后端匹配、状态或金额计算逻辑；页面只展示后端结果和必要说明。
- 导入、筛查和导出提交成功不等于业务完成，必须提示用户去任务中心核验最终状态。
- 任务状态使用 `pending`、`processing`、`completed`、`failed`、`cancelled`；失败时展示后端可公开的 `failure_reason`。
- 下载通过任务 UUID 的中转接口完成，不拼接或暴露服务端真实文件路径。

## 权限与安全

- 页面访问使用 `hasModulePermission`，操作按钮使用精确权限名；超级管理员和 `*` 权限按现有 `src/access.ts` 处理。
- 前端权限只控制体验，所有写操作和数据范围必须由后端最终校验。
- Token 当前保存在 `localStorage` 并通过 `Authorization: Bearer <token>` 发送；不得打印或写入文档。
- 不读取、提交或展示 `.env`、真实 Token、密码哈希、数据库口令和生产敏感配置。

## 文档维护

- 复杂功能变更前先更新对应 `README.md`、`业务流程图解.md`、`业务数据模型.md`。
- 功能发生实际变化时追加 `业务变更增量.md`，不得覆盖历史记录。
- 文档与代码冲突时以代码为准，并修正文档；帮助页用于解释业务，不可覆盖代码事实。
- 后端 `helps/未救助台账用户操作与核对参考文档.html` 和 `helps/参保台账用户操作与核对参考文档.html` 是两个台账最新用户说明的辅助来源。

## 变更与验证边界

- 只修改任务必要文件，不清理或格式化无关代码，不覆盖用户未提交改动。
- 未经明确要求不修改业务代码、配置、依赖、数据库结构，不提交、不推送、不改写 Git 历史。
- 纯文档改动检查 Markdown 结构、链接和 Mermaid 语法；涉及项目结构的文档变更后执行 `codegraph sync`。
- 代码任务按风险运行 `npm run format`、构建或相关检查；不得把未执行描述成已通过。

## CodeGraph

结构查询优先使用 CodeGraph，文本内容使用 `rg`。修改文档或代码并完成核对后执行 `codegraph sync`，确认索引成功后再交付。
