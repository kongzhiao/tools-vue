# 未救助台账业务变更增量

## 2026-07-12

- 新增：未救助明细与应补应退明细的筛查配置表首行增加“门诊重大疾病匹配”，两边保存和执行互不串用。
- 交互：规则可分别选择保留/剔除，配置医疗类别和指定病种编码，并明确提示自动匹配重大疾病编码库启用项。
- 结果：命中后状态为拟通知2并跳过后续规则；默认保留，配置剔除时仍保留拟通知2状态。
- 文案：相关页面用户可见“清洗”统一改为“筛查”；内部服务名、接口路径和权限标识保持兼容。
- 新增：建立未救助台账前端文档基线。
- 确认：未救助明细、应补应退明细与下放通知为三个独立业务节点。
- 确认：通知办理统一使用 `/unrescued/notice-records`，明细页只负责排查、筛查和导出。
- 确认：附件1/4、附件2、筛查、金额分档、镇街范围和通知状态与 2026-07-12 帮助页一致。
- 确认：导入只支持 CSV，耗时操作进入任务中心。
- 已知限制：页面仍存在未公开路由对应的存量方法和旧权限名，本次只记录现状，不改代码。
- 已知限制：身份证、联系方式和银行账户字段当前由页面直接展示，后续如调整脱敏需独立需求并同步前后端。
